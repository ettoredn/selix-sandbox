// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("selinux", "for selinux support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("selinux", "enable selinux support", "no");

if (PHP_SELINUX != "no") {
	EXTENSION("selinux", "selinux.c");
}

