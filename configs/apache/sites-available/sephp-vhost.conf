<VirtualHost *:80>
	DocumentRoot "${vhost_root}"
	<Directory "${vhost_root}">
		Options Indexes FollowSymLinks MultiViews
		AllowOverride All
		Order allow,deny
		allow from all
	</Directory>
	
	SetEnv SELINUX_DOMAIN	"sephp_t"
	SetEnv SELINUX_RANGE	"s0-s4:c1.c10"
</VirtualHost>
