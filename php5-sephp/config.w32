// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("sephp", "for sephp support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("sephp", "enable sephp support", "no");

if (PHP_SEPHP != "no") {
	EXTENSION("sephp", "sephp.c");
}

